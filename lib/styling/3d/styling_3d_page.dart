import 'package:flutter/material.dart';

class Styling3DPage extends StatelessWidget {
  const Styling3DPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('3D 모델링 작업 중입니다.'),
    );
  }
}
